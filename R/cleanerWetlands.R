#' \code{cleanerWetlands} package
#'
#' This package allows us to clean up data provided by regional councils before entering it into the MWLR databaese.
#'
#' @docType package
#' @name cleanerWetlands
NULL

## quiets concerns of R CMD check re: the .'s that appear in pipelines
if(getRversion() >= "2.15.1")  utils::globalVariables(c("."))